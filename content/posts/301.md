+++
title = "lnmp下关闭Mysql日志的方法"
date = "2013-12-31T19:30:00+08:00"
lastmod = "2021-03-24T20:36:09+08:00"
url = "/archives/301/"
typecho_cid = 301
typecho_slug = "301"
typecho_status = "publish"
category = ["O-M"]
categories = ["O-M"]
tag = ["旧博客数据", "linux"]
tags = ["旧博客数据", "linux"]
comments = true
toc = false
draft = false
+++
原链接：http://blog.pcwuyu.com/2013/364.html

原分类：网站建设, Linux

---

LNMP一键包安装的Mysql默认开启Mysql日志，如果网站对数据库读写较为频繁的话可能会产生大量日志，并占用磁盘空间，还有可能导致VPS的硬盘闲置空间消耗满导致数据库无法开启。

本教程的路径适用于LNMP一键安装包环境，其它环境请根据实际情况操作。

若需要关闭，请执行以下操作：


- 一、修改文件配置
 - 修改`/etc/my.cnf`，找到以下字符
`log-bin=mysql-bin` 
`binlog_format=mixed`
 - 注释这两行，如下：
`#log-bin=mysql-bin` 
`#binlog_format=mixed`
在这两个字段之前添加“#”号注释，并保存文件。

- 二、重启Mysql使其生效
`service mysqld restart`
SSH执行以上命令，重启Mysql使注释生效

- 三、删除之前产生的日志
 - 1）登陆MYSQL
`/usr/local/mysql/bin/mysql -u root -p`
SSH执行以上命令，输入密码后，进入MYSQL命令行模式。
 - 2）重设帐号
`reset typecho`        #typecho请修改为目标数据库名
